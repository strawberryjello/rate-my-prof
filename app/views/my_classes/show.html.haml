%p#notice= notice

%p
  %strong Name:
  = @my_class.name

%h3 Professors
%table
  %thead
    %tr
      %th Name
      %th Average Rating
      %th
  %tbody
    - @my_class.professors.each do |p|
      %tr
        %td= link_to p.full_name, professor_path(p)
        %td= p.average_rating_for(@my_class)
        %td= link_to 'Review This Professor', new_professor_review_path(p.id)

= link_to 'Edit', edit_my_class_path(@my_class) |
= link_to 'Back', my_classes_path
