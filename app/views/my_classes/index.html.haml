%p#notice= notice

%h1 My Classes

%table
  %thead
    %tr
      %th Name
      %th{:colspan => "3"}

  %tbody
    - @my_classes.each do |my_class|
      %tr
        %td= my_class.name
        %td= link_to 'Show', my_class
        %td= link_to 'Edit', edit_my_class_path(my_class)
        %td= link_to 'Destroy', my_class, method: :delete, data: { confirm: 'Are you sure?' }


%br/

= link_to 'New My Class', new_my_class_path
